# Core configuration
# Choose the LLM provider: gemini, openai, openrouter, or claude
LLM_PROVIDER=gemini

# Optional: override the embedding provider (google, openai, sentence-transformers)
# EMBEDDING_PROVIDER=

# Filesystem path where ChromaDB should persist data
# Defaults to ./chroma_db when unset
# CHROMA_DB_PATH=./chroma_db

# GitHub authentication
GITHUB_TOKEN=your_github_token_here

# Gemini configuration
# Required when LLM_PROVIDER=gemini or EMBEDDING_PROVIDER=google
# GEMINI_API_KEY=
# Optional overrides
# GEMINI_MODEL=gemini-2.5-pro
# GEMINI_EMBEDDING_MODEL=models/embedding-001

# OpenAI configuration
# Required when LLM_PROVIDER=openai or EMBEDDING_PROVIDER=openai
# OPENAI_API_KEY=
# Optional overrides
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_EMBEDDING_MODEL=text-embedding-3-large

# OpenRouter configuration
# Required when LLM_PROVIDER=openrouter
# OPENROUTER_API_KEY=
# Optional overrides
# OPENROUTER_MODEL=openrouter/auto
# OPENROUTER_APP_URL=https://your-app-url.example
# OPENROUTER_APP_NAME=AI Frontmatter Injector

# Anthropic (Claude) configuration
# Required when LLM_PROVIDER=claude
# ANTHROPIC_API_KEY=
# Optional override
# CLAUDE_MODEL=claude-3-5-sonnet-20240620

# Sentence-transformers configuration
# Used when EMBEDDING_PROVIDER=sentence-transformers
# SENTENCE_TRANSFORMER_MODEL=all-MiniLM-L6-v2
